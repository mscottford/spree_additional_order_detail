<%= form_for(@spree_aircraft_detail) do |f| %>
  <% if @spree_aircraft_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spree_aircraft_detail.errors.count, "error") %> prohibited this spree_aircraft_detail from being saved:</h2>

      <ul>
      <% @spree_aircraft_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :year %><br />
    <%= f.number_field :year %>
  </div>
  <div class="field">
    <%= f.label :aircraft_type %><br />
    <%= f.text_field :aircraft_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
